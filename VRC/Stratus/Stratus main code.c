#pragma config(Motor,  port1,           frontleft,     tmotorVex393, openLoop, reversed)
#pragma config(Motor,  port2,           frontright,    tmotorVex393, openLoop, reversed)
#pragma config(Motor,  port3,           backleft,      tmotorVex393, openLoop, reversed)
#pragma config(Motor,  port4,           backright,     tmotorVex393, openLoop, reversed)
#pragma config(Motor,  port5,           upright,       tmotorVex393, openLoop, reversed)
#pragma config(Motor,  port6,           upleft,        tmotorVex393, openLoop, reversed)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#pragma platform(VEX)

//Competition Control and Duration Settings
#pragma competitionControl(Competition)
#pragma autonomousDuration(20)
#pragma userControlDuration(120)

#include "Vex_Competition_Includes.c"   //Main competition background code...do not modify!

/////////////////////////////////////////////////////////////////////////////////////////
//
//                          Pre-Autonomous Functions
//
// You may want to perform some actions before the competition starts. Do them in the
// following function.
//
/////////////////////////////////////////////////////////////////////////////////////////

void pre_auton()
{
  // Set bStopTasksBetweenModes to false if you want to keep user created tasks running between
  // Autonomous and Tele-Op modes. You will need to manage all user created tasks if set to false.
  bStopTasksBetweenModes = true;

	// All activities that occur before the competition starts
	// Example: clearing encoders, setting servo positions, ...
}

/////////////////////////////////////////////////////////////////////////////////////////
//
//                                 Autonomous Task
//
// This task is used to control your robot during the autonomous phase of a VEX Competition.
// You must modify the code to add your own robot specific commands here.
//
/////////////////////////////////////////////////////////////////////////////////////////

task autonomous()
{ motor(frontleft)= 100; wait1Msec(100);
	motor(frontright)= 100; wait1Msec(100);
	motor(backleft)= 100; wait1Msec(100);
	motor(backright)= 100; wait1Msec(100);
	AutonomousCodePlaceholderForTesting();  // Remove this function call once you have "real" code.
}

/////////////////////////////////////////////////////////////////////////////////////////
//
//                                 User Control Task
//
// This task is used to control your robot during the user control phase of a VEX Competition.
// You must modify the code to add your own robot specific commands here.
//
/////////////////////////////////////////////////////////////////////////////////////////

task usercontrol()
{
	// User control code here, inside the loop
	int threshold = 10;
	while (true)
	{
 	  getJoystickSettings(joystick);
 	      // update buttons and joysticks
  		if(abs(joystick.joy1_y2 > threshold)
  	{
  			motor[frontright] = joystick.joy1_y2;
  			motor[frontleft] = -joystick.joy1_y2;
  	}
		 else
	 {
				motor[frontright] = 0;
				motor[frontleft] = 0;
	 }


	 if(abs(joystick.joy1_y1 > threshold)
  	{
  			motor[frontright] = -joystick.joy1_y2;
  			motor[frontleft] = joystick.joy1_y2;
  	}
		 else
	 {
				motor[frontright] = 0;
				motor[frontleft] = 0;
	 }

	  UserControlCodePlaceholderForTesting(); // Remove this function call once you have "real" code.
	}
}
